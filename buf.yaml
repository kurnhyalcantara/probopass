version: v2
modules:
  - path: proto
deps:
  # Provides google/api/annotations.proto for grpc-gateway HTTP bindings.
  - buf.build/googleapis/googleapis
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
